# Halo 部署文档

## 环境要求

在构建和部署 Halo 之前，请确保您已安装以下软件：

- Java 21
- Node.js v20.19.0
- pnpm v10.12.4
- Docker 和 Docker Compose

## 常用命令

### 项目构建命令

```bash
# 清理并完整构建（推荐）
./gradlew clean downloadPluginPresets build -x test -x ui:lint

```bash
./gradlew clean downloadPluginPresets build -x test -x ui:lint -x javadoc -Dhttp.proxyHost=127.0.0.1 -Dhttp.proxyPort=12334  -Dhttps.proxyHost=127.0.0.1 -Dhttps.proxyPort=12334
```

```bash
# 仅构建 JAR 包（跳过测试和检查）
./gradlew clean downloadPluginPresets bootJar -x test -x ui:lint -x checkstyleMain
```

```bash
# 并行构建（提高速度）
./gradlew clean downloadPluginPresets build -x test -x ui:lint --parallel --build-cache
```

```bash
# 下载插件预设
./gradlew downloadPluginPresets
```

```bash
# 仅构建前端
./gradlew :ui:doBuild
```

```bash
# 创建校验和
./gradlew createChecksums
```

### Docker 相关命令

```bash
# 构建本地 Docker 镜像
docker build . -t halo:local

# 查看容器状态
docker-compose ps

# 查看容器日志
docker-compose logs gordon-blog
docker-compose logs gordon-blog --tail=50
docker-compose logs -f gordon-blog  # 实时查看

# 重启服务
docker-compose restart
docker-compose restart gordon-blog

# 停止并删除容器
docker-compose down

# 停止、删除容器并清理卷
docker-compose down -v

# 重新构建并启动
docker-compose build --no-cache
docker-compose up -d

# 进入容器
docker exec -it gordon-blog /bin/sh
docker exec -it gordon-db /bin/sh
```

### 数据库操作命令

```bash
# PostgreSQL 数据库操作
docker exec gordon-db psql -U gordon -d gordon -c "SELECT version();"
docker exec gordon-db psql -U gordon -d gordon -c "\dt"
docker exec gordon-db psql -U gordon -d gordon -c "\l"

# 数据库备份
docker exec gordon-db pg_dump -U gordon gordon > backup.sql

# 数据库恢复
docker exec -i gordon-db psql -U gordon gordon < backup.sql
```

### 系统监控命令

```bash
# 检查端口占用
netstat -tlnp | grep 8103
ss -tlnp | grep 8103
lsof -i :8103

# 检查进程
ps aux | grep halo

# 检查内存使用
free -h
docker stats

# 检查磁盘使用
df -h
du -sh ./xblog ./db
```

### 网络测试命令

```bash
# 测试应用访问
curl http://localhost:8103
curl -I http://localhost:8103
curl -v http://localhost:8103

# 测试健康检查
curl http://localhost:8103/actuator/health
curl http://localhost:8103/actuator/health/readiness

# 测试数据库连接
curl http://localhost:8103/actuator/health

# 跟随重定向
curl -L http://localhost:8103

# 测试端口连通性
telnet localhost 8103
nc -zv localhost 8103
```

### Nginx 相关命令

```bash
# 测试 Nginx 配置
sudo nginx -t

# 重启 Nginx
sudo systemctl restart nginx
sudo systemctl reload nginx

# 查看 Nginx 状态
sudo systemctl status nginx

# 查看 Nginx 日志
sudo tail -f /var/log/nginx/access.log
sudo tail -f /var/log/nginx/error.log
```

### 文件和权限管理

```bash
# 查看挂载目录
ls -la ./xblog/
ls -la ./db/

# 创建必要目录
mkdir -p ./xblog ./db

# 设置目录权限
chmod -R 755 ./xblog ./db
chown -R 1000:1000 ./xblog

# 查看 JAR 包内容
jar -tf application/build/libs/halo-*.jar | grep "presets/plugins"

# 查找文件
find ./xblog/ -name "*.yml" -o -name "*.yaml" -o -name "*.properties"
find ./xblog/ -type f -name "*" | head -20
```

### Git 操作命令

```bash
# 克隆项目
git clone https://github.com/halo-dev/halo.git
cd halo

# 更新代码
git pull origin main

# 查看当前状态
git status
git log --oneline -10

# 切换分支
git checkout -b feature/my-feature
git checkout main
```

## 构建项目

### 1. 克隆代码仓库

```bash
git clone https://github.com/halo-dev/halo.git
cd halo
```

### 2. 构建应用程序

运行以下命令构建整个项目（排除 UI 代码检查）：

```bash
./gradlew clean downloadPluginPresets build -x ui:lint
```

此命令将：
- 编译所有 Java 源代码
- 下载预设插件
- 运行测试
- 将应用程序打包成 JAR 文件
- 构建前端资源

构建完成后，JAR 文件将位于 `application/build/libs/halo.jar`。

### 3. 构建 Docker 镜像（可选）

如果您想构建用于部署的 Docker 镜像：

```bash
docker build . -t ghcr.io/halo-dev/halo-dev:main
```

## 部署选项

Halo 支持使用 Docker Compose 的多种部署方式。您可以根据需求选择合适的数据库。

### 方式一：独立部署（嵌入式数据库）

创建 `docker-compose.yaml` 文件：

```yaml
version: '3.1'
services:
  halo:
    image: ghcr.io/halo-dev/halo-dev:main
    restart: always
    ports:
      - 8090:8090
    volumes:
      - ./halo:/root/.halo2
```

启动服务：

```bash
docker-compose up -d
```

### 方式二：使用 MySQL 数据库

创建 `docker-compose-mysql.yaml` 文件：

```yaml
version: '3.1'
services:
  halo:
    image: ghcr.io/halo-dev/halo-dev:main
    restart: always
    ports:
      - 8090:8090
    volumes:
      - ./halo:/root/.halo2
    command:
      - --spring.r2dbc.url=r2dbc:pool:mysql://mysql:3306/halo
      - --spring.r2dbc.username=root
      - --spring.r2dbc.password=halo
      - --spring.sql.init.platform=mysql
    depends_on:
      mysql:
        condition: service_healthy

  mysql:
    image: mysql:8.1.0
    restart: always
    environment:
      - MYSQL_ROOT_PASSWORD=halo
      - MYSQL_DATABASE=halo
    volumes:
      - ./mysql:/var/lib/mysql
    healthcheck:
      test: ["CMD", "mysqladmin", "ping", "-h", "127.0.0.1", "--silent"]
      interval: 10s
      timeout: 5s
      retries: 5
```

启动服务：

```bash
docker-compose -f docker-compose-mysql.yaml up -d
```

### 方式三：使用 PostgreSQL 数据库

创建 `docker-compose-postgres.yaml` 文件：

```yaml
version: '3.1'
services:
  halo:
    image: ghcr.io/halo-dev/halo-dev:main
    restart: always
    ports:
      - 8090:8090
    volumes:
      - ./halo:/root/.halo2
    command:
      - --spring.r2dbc.url=r2dbc:pool:postgresql://postgres/halo
      - --spring.r2dbc.username=halo
      - --spring.r2dbc.password=openpostgresql
      - --spring.sql.init.platform=postgresql
    depends_on:
      postgres:
        condition: service_healthy

  postgres:
    image: postgres:15.4
    restart: always
    environment:
      - POSTGRES_PASSWORD=openpostgresql
      - POSTGRES_USER=halo
      - POSTGRES_DB=halo
    volumes:
      - ./postgres:/var/lib/postgresql/data
    healthcheck:
      test: [ "CMD", "pg_isready" ]
      interval: 10s
      timeout: 5s
      retries: 5
```

启动服务：

```bash
docker-compose -f docker-compose-postgres.yaml up -d
```

## 配置选项

Halo 可以通过命令行参数或配置文件进行配置。主要配置选项包括：

- 数据库连接设置
- 外部访问地址
- 管理员用户初始化
- 插件目录

在您的挂载目录（`./halo`）中创建 `application.yaml` 文件来自定义设置：

```yaml
server:
  port: 8090

spring:
  r2dbc:
    url: r2dbc:pool:h2:file:///root/.halo2/db/halo
    username: sa
    password: ""
  sql:
    init:
      platform: h2

halo:
  external-url: http://localhost:8090/
```

## 访问应用

服务启动后，您可以通过以下地址访问 Halo：

- 管理后台：`http://your-domain:8090`
- 前台网站：`http://your-domain:8090`（初始设置完成后）

首次访问将重定向到安装页面，在那里您可以配置管理员账户和基本站点设置。

## 更新 Halo

要更新到新版本：

1. 拉取最新的 Docker 镜像：
   ```bash
   docker pull ghcr.io/halo-dev/halo-dev:main
   ```

2. 停止正在运行的容器：
   ```bash
   docker-compose down
   ```

3. 使用新镜像启动服务：
   ```bash
   docker-compose up -d
   ```

## 备份和恢复

### 备份

要备份数据，只需复制挂载的卷目录：

```bash
# 备份应用数据
tar -czf halo-backup-$(date +%Y%m%d).tar.gz ./halo

# 备份数据库（PostgreSQL）
docker exec gordon-db pg_dump -U gordon gordon > halo-db-backup-$(date +%Y%m%d).sql

# 完整备份
tar -czf halo-full-backup-$(date +%Y%m%d).tar.gz ./halo ./db docker-compose.yml .env
```

### 恢复

要从备份恢复，请在启动容器前将卷目录替换为您的备份数据：

```bash
# 恢复应用数据
tar -xzf halo-backup-20231201.tar.gz

# 恢复数据库
docker exec -i gordon-db psql -U gordon gordon < halo-db-backup-20231201.sql
```

## 故障排除

### 常用排查命令

```bash
# 检查容器状态
docker-compose ps

# 查看详细日志
docker-compose logs gordon-blog --tail=100

# 检查端口占用
netstat -tlnp | grep 8103

# 检查防火墙
sudo ufw status
sudo firewall-cmd --list-ports

# 检查磁盘空间
df -h

# 检查 Docker 网络
docker network ls
docker network inspect gordon-net
```

### 常见问题

1. **端口冲突**：如果 8090 端口已被占用，请在 docker-compose 文件中更改端口映射。

2. **数据库连接问题**：确保 Halo 配置中的数据库凭据与数据库服务匹配。

3. **内存不足**：如果应用程序意外崩溃，请增加 Docker 的内存分配。

4. **权限问题**：确保 Docker 对挂载卷具有适当的访问权限。

5. **404 错误**：检查应用是否正在重定向到设置页面，使用浏览器访问完成初始化。

## 性能优化

```bash
# 清理 Docker 缓存
docker system prune -a

# 清理 Gradle 缓存
./gradlew clean

# 监控资源使用
docker stats

# 查看应用性能指标
curl http://localhost:8103/actuator/metrics
```

## 高级配置

对于更高级的配置，请参考官方文档：[https://halo.run](https://halo.run)

本文档涵盖了使用 Docker Compose 构建和部署 Halo 的基本步骤和常用命令。有关特定配置或故障排除，请查阅项目的官方文档或社区支持渠道。