# Halo 部署文档

## 环境要求

在构建和部署 Halo 之前，请确保您已安装以下软件：

- **Java**: JDK 21
- **Node.js**: v20.19.0
- **pnpm**: v10.12.4
- **Docker & Docker Compose**

---

## ⚠️ 重要提示：前端构建必须成功

**Halo 是前后端一体化项目，JAR 包中必须包含前端控制台文件（`console/index.html` 等）才能正常访问后台管理界面。**

### 关键点：

1. **必须使用 `build` 任务**，不能只用 `bootJar`
2. **前端构建失败时，JAR 包会缺失控制台文件**，导致访问 `/console` 时报错：
   ```
   java.io.FileNotFoundException: class path resource [console/index.html] cannot be opened because it does not exist
   ```
3. **构建前确保 `ui/node_modules` 完整**，依赖缺失或版本冲突会导致前端构建失败

### 验证 JAR 是否包含前端

构建完成后，务必验证 JAR 包是否包含控制台文件：

**Windows (PowerShell)**:
```powershell
# 检查 JAR 内容
jar -tf application\build\libs\halo-2.22.0-SNAPSHOT.jar | findstr "console/index.html"

# 应该输出：
# BOOT-INF/classes/console/index.html
```

**Linux / macOS**:
```bash
# 检查 JAR 内容
jar -tf application/build/libs/halo-2.22.0-SNAPSHOT. jar | grep "console/index.html"

# 应该输出：
# BOOT-INF/classes/console/index.html
```

**如果没有输出，说明前端未打包进 JAR，需要重新构建！**

---

## 常用命令

### 项目构建命令

> **注意**：由于前端构建可能会触发 TypeScript 类型检查报错或网络超时，建议在构建命令中显式跳过非必要检查。

#### ✅ 推荐：完整构建（包含前端）

```bash
# 清理并完整构建（推荐，包含跳过前端 Lint 和 Typecheck）
./gradlew clean downloadPluginPresets build -x test -x ui:lint -x ui:typecheck
```

**说明**：
- `build` 任务会构建所有模块，包括前端（`ui` 模块）
- `-x ui:lint` 和 `-x ui:typecheck` 只是跳过代码检查，**不影响前端编译和打包**
- 前端会被编译并打包到 `application/build/libs/halo-*. jar` 中

#### ❌ 不推荐：仅构建 JAR（可能缺少前端）

```bash
# 仅构建 JAR 包（跳过测试、检查和文档，最快速度）
./gradlew clean downloadPluginPresets bootJar -x test -x ui:lint -x ui:typecheck -x checkstyleMain -x javadoc
```

**警告**：使用 `bootJar` 任务可能导致前端未编译，建议只在确认前端已构建的情况下使用。

#### 网络环境较差时的构建

```bash
# 配置代理构建（解决前端依赖拉取慢的问题）
./gradlew clean downloadPluginPresets build -x test -x ui:lint -x ui:typecheck \
  -Dhttp.proxyHost=127.0.0.1 -Dhttp.proxyPort=7890 \
  -Dhttps.proxyHost=127.0.0.1 -Dhttps.proxyPort=7890
```

#### 仅重新构建前端

```bash
# 仅重新构建前端资源（用于前端开发调试）
./gradlew :ui:clean :ui:pnpmInstall :ui:buildPackages

# 然后重新打包到 JAR
./gradlew : application:bootJar -x test
```

### Docker 相关命令

```bash
# 构建本地 Docker 镜像
docker build . -t halo:local

# 启动容器（后台运行）
docker compose up -d

# 强制重新构建并启动（修改代码或配置后推荐使用）
docker compose down
docker compose up -d --build

# 彻底清理卷并重新构建（慎用，会删除数据库数据）
docker compose down -v && docker compose up -d --build

sudo docker compose up -d --build --force-recreate

# 查看容器日志（实时）
docker compose logs -f gordon-blog

# 重启服务
docker compose restart gordon-blog
```

### 数据库操作命令

```bash
# 进入 PostgreSQL 命令行
docker exec -it gordon-db psql -U halo -d halo

# 数据库备份 (PostgreSQL)
docker exec gordon-db pg_dump -U halo halo > backup_$(date +%Y%m%d).sql

# 数据库恢复 (PostgreSQL)
cat backup.sql | docker exec -i gordon-db psql -U halo halo
```

---

## 常见构建问题与修复

### 🔥 1. 前端构建失败导致 JAR 缺少控制台文件

**现象**：
- 构建过程中 `:ui:build` 或 `:ui:buildPackages` 任务失败
- 访问 `/console` 时报错：`FileNotFoundException: console/index.html cannot be opened`
- 日志中出现：`Attachment handler is unavailable`

**根本原因**：
前端构建失败时，Gradle 仍可能继续构建后端并生成 JAR，但 JAR 中不包含前端资源。

**排查步骤**：

1. **检查构建日志，确认前端构建是否成功**：
   ```bash
   ./gradlew clean build -x test -x ui:lint -x ui:typecheck
   
   # 查看日志中是否有类似输出：
   # > Task :ui:pnpmInstall
   # > Task :ui:buildPackages
   # > Task :ui:build
   # BUILD SUCCESSFUL
   ```

2. **检查前端依赖是否完整**：
   ```bash
   # 检查 ui/node_modules 是否存在
   ls -la ui/node_modules
   
   # 检查关键包是否存在
   ls ui/node_modules/. pnpm/ | grep -E "ansis|esbuild|vite"
   ```

3. **验证 JAR 包内容**：
   ```bash
   jar -tf application/build/libs/halo-2.22.0-SNAPSHOT.jar | grep "console"
   
   # 应该看到大量输出，包括：
   # BOOT-INF/classes/console/index.html
   # BOOT-INF/classes/console/assets/... 
   # 如果没有输出，说明前端未打包！
   ```

**解决方法**：

**Windows (PowerShell)**:
```powershell
# 1. 清理前端依赖
cd ui
Remove-Item -Recurse -Force node_modules -ErrorAction SilentlyContinue
Remove-Item pnpm-lock.yaml -ErrorAction SilentlyContinue

# 2. 清理 pnpm 缓存
pnpm store prune

# 3. 重新安装依赖
pnpm install

# 4. 返回根目录
cd ..

# 5. 完整重新构建
.\gradlew clean downloadPluginPresets build -x test -x ui:lint -x ui:typecheck

# 6. 验证 JAR
jar -tf application\build\libs\halo-2.22.0-SNAPSHOT. jar | findstr "console/index.html"
```

**Linux / macOS**:
```bash
# 1. 清理前端依赖
cd ui
rm -rf node_modules pnpm-lock.yaml

# 2. 清理 pnpm 缓存
pnpm store prune

# 3. 重新安装依赖
pnpm install

# 4. 返回根目录
cd ..

# 5. 完整重新构建
./gradlew clean downloadPluginPresets build -x test -x ui:lint -x ui:typecheck

# 6. 验证 JAR
jar -tf application/build/libs/halo-2.22.0-SNAPSHOT. jar | grep "console/index. html"
```

### 2. 前端依赖安装失败：`ERR_MODULE_NOT_FOUND:  Cannot find package 'ansis'`

**现象**：
```
packages/api-client build:  Error [ERR_MODULE_NOT_FOUND]:  Cannot find package 'ansis' imported from ... 
```

**原因**：
`node_modules` 损坏或 pnpm 缓存不一致。

**解决方法**：
```bash
cd ui
rm -rf node_modules pnpm-lock.yaml
pnpm store prune
pnpm install
cd ..
./gradlew clean build -x test -x ui:lint -x ui:typecheck
```

### 3. esbuild 版本不匹配：`Host version does not match binary version`

**现象**：
```
[ERROR] Cannot start service:  Host version "0.21.5" does not match binary version "0.25.5"
```

**原因**：
esbuild 的 Node.js 模块和二进制文件版本不一致，通常是缓存或依赖锁定问题。

**解决方法**：
```bash
cd ui
rm -rf node_modules/. pnpm/esbuild*
rm -rf node_modules/esbuild
pnpm install --force
cd ..
./gradlew : ui:buildPackages
```

### 4. vite 可执行文件丢失或 `node_modules` 损坏

**现象**：
构建过程中报错 `Process 'command 'pnpm. cmd'' finished with non-zero exit value 1`，或者提示找不到 vite。

**解决方法**：
彻底删除前端依赖目录，强制 pnpm 重新拉取并安装。

**Windows (PowerShell)**:
```powershell
Remove-Item -Recurse -Force ui\node_modules
Remove-Item -Recurse -Force ui\packages\components\node_modules

# 重新执行安装和构建
./gradlew clean downloadPluginPresets : ui:pnpmInstall :ui:buildPackages build -x test -x ui:lint -x ui:typecheck
```

### 5. TypeScript 类型检查失败 (`:ui:typecheck`)

**现象**：
Vite 构建成功，但卡在 `:ui:typecheck` 任务，报错 `Execution failed for task ':ui:typecheck'`。

**解决方法**：
在构建命令中添加 `-x ui:typecheck` 跳过此步骤（不影响最终运行）。

或者永久禁用（修改 `ui/build.gradle`）：
```groovy
tasks.named('typecheck') {
    enabled = false
}
```

### 6. Java 编译报错：`[removal] ...  已过时` 且 `-Werror` 开启

**现象**：
构建失败并提示：`warnings found and -Werror specified`，通常涉及 `[removal]` 类型的过时 API 警告。

**解决方法**：
项目默认将警告视为错误。需在 `api/build.gradle`（或根目录 build.gradle 的 subprojects 块）中配置编译器参数，移除 `-Werror`。

```groovy
subprojects {
    tasks.withType(JavaCompile).configureEach {
        // 关键：移除 -Werror 参数，允许警告存在
        options.compilerArgs. removeAll { it == '-Werror' }
        // 可选：添加 -Xlint:-removal 尝试抑制特定警告
        options.compilerArgs += ["-Xlint:-removal"]
    }
}
```

### 7. 网络问题导致前端依赖下载失败

**现象**：
pnpm install 卡住或超时。

**解决方法**：

**配置 pnpm 镜像**：
```bash
# 使用淘宝镜像
pnpm config set registry https://registry.npmmirror.com

# 或恢复官方源
pnpm config set registry https://registry.npmjs.org
```

**配置代理**（如果使用代理）：
```bash
# 在 ui 目录创建 .npmrc
cd ui
cat > .npmrc << EOF
proxy=http://127.0.0.1:7890
https-proxy=http://127.0.0.1:7890
EOF

pnpm install
```

---

## 🚀 完整构建流程（推荐）

### 第一次构建或依赖变更后

```bash
# 1. 停止 Gradle 守护进程
./gradlew --stop

# 2. 清理前端依赖（可选，依赖有问题时执行）
cd ui
rm -rf node_modules pnpm-lock.yaml
pnpm store prune
pnpm install
cd ..

# 3. 完整构建
./gradlew clean downloadPluginPresets build -x test -x ui:lint -x ui:typecheck

# 4. 验证 JAR 包含前端
jar -tf application/build/libs/halo-2.22.0-SNAPSHOT.jar | grep "console/index.html"

# 5. 查看生成的 JAR
ls -lh application/build/libs/
```

### 日常开发构建

```bash
# 快速构建（依赖已安装）
./gradlew build -x test -x ui:lint -x ui:typecheck

# 验证
jar -tf application/build/libs/halo-*. jar | grep "console/index. html"
```

### Docker 部署流程

```bash
# 1. 构建 JAR
./gradlew clean build -x test -x ui:lint -x ui:typecheck

# 2. 验证 JAR
jar -tf application/build/libs/halo-*.jar | grep "console/index.html"

# 3. 复制 JAR 到部署目录（如果需要）
cp application/build/libs/halo-2.22.0-SNAPSHOT. jar /path/to/deploy/

# 4. 构建 Docker 镜像
docker compose build --no-cache

# 5. 启动容器
docker compose up -d

# 6. 查看日志
docker compose logs -f gordon-blog

# 7. 验证访问
curl http://localhost:8103/console
# 应该返回 HTML 内容，而不是 404 或 500 错误
```

---

## 附件存储与插件问题（重要）

### 报错：`Attachment handler is unavailable`

**现象**：
启动后查看日志出现大量警告，无法上传附件或头像显示异常：
```log
...  AttachmentReconciler-worker-1: Requeue Request ...  due to Attachment handler is unavailable, requeue the request
...  UserReconciler-worker-1: ...  Avatar permalink ...  is not available yet
```

**原因**：
Halo 2.x 核心不再内置本地存储策略，必须依赖外部插件。此报错通常是因为 `plugins` 目录为空，或者缺少必要的存储插件（如 `plugin-s3`）。

**解决方法**：
手动下载存储插件并放入挂载的 `plugins` 目录中，然后重启容器。

1. **确认插件目录位置**：
   根据 `docker-compose.yaml` 中的挂载路径，找到宿主机的 `plugins` 文件夹（例如 `/home/ubuntu/xblog/xblog/plugins`）。

2. **下载插件**：
   下载支持本地存储的 S3 插件（推荐 `plugin-s3`）。
   ```bash
   cd /home/ubuntu/xblog/xblog/plugins
   wget https://github.com/halo-sigs/plugin-s3/releases/download/v1.1.0/plugin-s3-1.1.0.jar
   # 确保有读取权限
   chmod 644 plugin-s3-1.1.0.jar
   ```

3. **重启服务**：
   ```bash
   docker compose restart gordon-blog
   ```

4. **配置插件**：
   登录 Console 后台 -> 插件 -> 启用 `plugin-s3` -> 设置 -> 选择存储类型为 `Local`。

---

## 部署配置参考

### Docker Compose (PostgreSQL 版)

推荐使用 PostgreSQL 作为生产环境数据库。

`docker-compose.yaml`:

```yaml
services:
  gordon-blog: 
    build:
      context: .
    container_name: gordon-blog
    restart: unless-stopped
    depends_on:
      gordon-db:
        condition: service_healthy
    networks:
      - gordon-net
    ports:
      - "${EXTERNAL_PORT:-8103}:8090"
    volumes:
      - ./xblog:/application/xblog2  # 宿主机 ./xblog 将包含 plugins, themes, logs 等目录
    environment:
      JVM_OPTS: "${JVM_OPTS: --Xmx256m -Xms256m}"
      TZ: "Asia/Shanghai"
      # 确保容器内工作目录与挂载点一致
      HALO_WORK_DIR: "/application/xblog2"
    command:
      - --spring.r2dbc.url=r2dbc:pool:postgresql://gordon-db/${POSTGRES_DB}
      - --spring.r2dbc.username=${POSTGRES_USER}
      - --spring. r2dbc.password=${POSTGRES_PASSWORD}
      - --spring.sql.init.platform=postgresql
      - --halo.external-url=http://localhost:${EXTERNAL_PORT:-8103}/
      - --halo.work-dir=/application/xblog2
    healthcheck:
      test: ["CMD", "curl", "-f", "http://localhost:8090/actuator/health/readiness"]
      interval: 30s
      timeout: 10s
      retries: 5
      start_period: 60s
    deploy:
      resources:
        limits:
          memory: 320M
        reservations:
          memory: 64M

  gordon-db:
    image:  postgres:15-alpine
    container_name: gordon-db
    restart: unless-stopped
    ports:
      - "${DB_PORT:-15432}:5432"
    networks:
      - gordon-net
    volumes:
      - ./db:/var/lib/postgresql/data
    environment:
      POSTGRES_USER: ${POSTGRES_USER}
      POSTGRES_PASSWORD: ${POSTGRES_PASSWORD}
      POSTGRES_DB: ${POSTGRES_DB}
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U ${POSTGRES_USER:-gordon}"]
      interval: 10s
      timeout: 5s
      retries: 5
      start_period: 30s
    deploy:
      resources:
        limits:
          memory: 256M
        reservations:
          memory: 64M

networks: 
  gordon-net:
    name: gordon-net
    driver: bridge
```

### 启动服务

```bash
# 首次启动或配置变更后
docker compose up -d --build
```

访问地址：[http://localhost:8103](http://localhost:8103)

---

## 维护与排查

### 检查端口占用
如果端口启动失败，检查占用情况：
```bash
# Linux / macOS
lsof -i :8103
# Windows PowerShell
netstat -ano | findstr 8103
```

### 资源监控
查看 Halo 的实时资源占用：
```bash
docker stats gordon-blog
```

### 查看完整日志
```bash
# 实时日志
docker compose logs -f gordon-blog

# 最近 100 行
docker compose logs --tail=100 gordon-blog

# 查找错误
docker compose logs gordon-blog | grep -i error
```

### 进入容器调试
```bash
# 进入容器
docker exec -it gordon-blog sh

# 查看 JAR 包内容
jar -tf app.jar | grep console

# 查看工作目录
ls -la /application/xblog2/
```

---

## 快速排查清单

遇到 `/console` 访问 404 或 500 错误时，按以下顺序排查：

- [ ] 1. 检查构建日志，确认 `:ui:build` 任务是否成功
- [ ] 2. 验证 JAR 包：`jar -tf application/build/libs/halo-*.jar | grep console/index.html`
- [ ] 3. 检查 `ui/node_modules` 是否完整
- [ ] 4. 清理并重新安装前端依赖：`cd ui && rm -rf node_modules && pnpm install`
- [ ] 5. 完整重新构建：`./gradlew clean build -x test -x ui:lint -x ui:typecheck`
- [ ] 6. 重新构建 Docker 镜像：`docker compose build --no-cache`
- [ ] 7. 检查容器日志：`docker compose logs -f gordon-blog`
- [ ] 8. 进入容器验证：`docker exec -it gordon-blog jar -tf app.jar | grep console`

---

## 推荐：使用官方镜像

如果自行构建遇到问题，可以使用官方预构建镜像：

```yaml
services:
  gordon-blog:
    image: halohub/halo:2.22.0  # 使用官方镜像
    # 注释掉 build 配置
    # build:
    #   context: .
    container_name: gordon-blog
    # ...  其他配置保持不变
```

```bash
docker compose pull
docker compose up -d
```

---

## 总结

**关键要点**：
1. ✅ 必须使用 `build` 任务，确保前端被编译
2. ✅ 构建后验证 JAR 包含 `console/index.html`
3. ✅ 前端依赖问题时，删除 `node_modules` 重新安装
4. ✅ Docker 部署前验证 JAR，避免打包错误的镜像
5. ✅ 遇到问题时查看完整构建日志，定位失败的任务

**核心命令**：
```bash
# 完整构建
./gradlew clean downloadPluginPresets build -x test -x ui:lint -x ui:typecheck

# 验证 JAR
jar -tf application/build/libs/halo-*.jar | grep "console/index.html"
```