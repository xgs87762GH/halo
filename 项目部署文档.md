# Halo 部署文档

## 环境要求

在构建和部署 Halo 之前，请确保您已安装以下软件：

- **Java**: JDK 21
- **Node.js**: v20.19.0
- **pnpm**: v10.12.4
- **Docker & Docker Compose**

## 常用命令

### 项目构建命令

> **注意**：由于前端构建可能会触发 TypeScript 类型检查报错或网络超时，建议在构建命令中显式跳过非必要检查。

```bash
# 清理并完整构建（推荐，包含跳过前端 Lint 和 Typecheck）
./gradlew clean downloadPluginPresets build -x test -x ui:lint -x ui:typecheck
```

```bash
# 仅构建 JAR 包（跳过测试、检查和文档，最快速度）
./gradlew clean downloadPluginPresets bootJar -x test -x ui:lint -x ui:typecheck -x checkstyleMain -x javadoc
```

```bash
# 网络环境较差时，配置代理构建（解决前端依赖拉取慢的问题）
./gradlew clean downloadPluginPresets build -x test -x ui:lint -x ui:typecheck `
  -Dhttp.proxyHost=127.0.0.1 -Dhttp.proxyPort=7890 `
  -Dhttps.proxyHost=127.0.0.1 -Dhttps.proxyPort=7890
```

```bash
# 仅重新构建前端资源（用于前端开发调试）
./gradlew :ui:clean :ui:pnpmInstall :ui:buildPackages
```

### Docker 相关命令

```bash
# 构建本地 Docker 镜像
docker build . -t halo:local

# 启动容器（后台运行）
docker compose up -d

# 强制重新构建并启动（修改代码或配置后推荐使用）
docker compose down
docker compose up -d --build

# 彻底清理卷并重新构建（慎用，会删除数据库数据）
docker compose down -v && docker compose up -d --build

sudo docker compose up -d --build --force-recreate

# 查看容器日志（实时）
docker compose logs -f halo

# 重启服务
docker compose restart halo
```

### 数据库操作命令

```bash
# 进入 PostgreSQL 命令行
docker exec -it halo-db psql -U halo -d halo

# 数据库备份 (PostgreSQL)
docker exec halo-db pg_dump -U halo halo > backup_$(date +%Y%m%d).sql

# 数据库恢复 (PostgreSQL)
cat backup.sql | docker exec -i halo-db psql -U halo halo
```

## 常见构建问题与修复

### 1. 前端构建失败：`vite` 可执行文件丢失或 `node_modules` 损坏

**现象**：
构建过程中报错 `Process 'command 'pnpm.cmd'' finished with non-zero exit value 1`，或者提示找不到 vite。

**解决方法**：
彻底删除前端依赖目录，强制 pnpm 重新拉取并安装。

**Windows (PowerShell)**:
```powershell
Remove-Item -Recurse -Force ui\node_modules
Remove-Item -Recurse -Force ui\packages\components\node_modules

# 重新执行安装和构建
./gradlew clean downloadPluginPresets :ui:pnpmInstall :ui:buildPackages build -x test -x ui:lint -x ui:typecheck
```

### 2. TypeScript 类型检查失败 (`:ui:typecheck`)

**现象**：
Vite 构建成功，但卡在 `:ui:typecheck` 任务，报错 `Execution failed for task ':ui:typecheck'`。

**解决方法**：
在构建命令中添加 `-x ui:typecheck` 跳过此步骤（不影响最终运行）。

或者永久禁用（修改 `ui/build.gradle`）：
```groovy
tasks.named('typecheck') {
    enabled = false
}
```

### 3. Java 编译报错：`[removal] ... 已过时` 且 `-Werror` 开启

**现象**：
构建失败并提示：`warnings found and -Werror specified`，通常涉及 `[removal]` 类型的过时 API 警告。

**解决方法**：
项目默认将警告视为错误。需在 `api/build.gradle`（或根目录 build.gradle 的 subprojects 块）中配置编译器参数，移除 `-Werror`。

```groovy
subprojects {
    tasks.withType(JavaCompile).configureEach {
        // 关键：移除 -Werror 参数，允许警告存在
        options.compilerArgs.removeAll { it == '-Werror' }
        // 可选：添加 -Xlint:-removal 尝试抑制特定警告
        options.compilerArgs += ["-Xlint:-removal"]
    }
}
```

---

## 附件存储与插件问题（重要）

### 报错：`Attachment handler is unavailable`

**现象**：
启动后查看日志出现大量警告，无法上传附件或头像显示异常：
```log
... AttachmentReconciler-worker-1: Requeue Request ... due to Attachment handler is unavailable, requeue the request
... UserReconciler-worker-1: ... Avatar permalink ... is not available yet
```

**原因**：
Halo 2.x 核心不再内置本地存储策略，必须依赖外部插件。此报错通常是因为 `plugins` 目录为空，或者缺少必要的存储插件（如 `plugin-s3`）。

**解决方法**：
手动下载存储插件并放入挂载的 `plugins` 目录中，然后重启容器。

1.  **确认插件目录位置**：
    根据 `docker-compose.yaml` 中的挂载路径，找到宿主机的 `plugins` 文件夹（例如 `/home/ubuntu/xblog/xblog/plugins`）。

2.  **下载插件**：
    下载支持本地存储的 S3 插件（推荐 `plugin-s3`）。
    ```bash
    cd /home/ubuntu/xblog/xblog/plugins
    wget https://github.com/halo-sigs/plugin-s3/releases/download/v1.1.0/plugin-s3-1.1.0.jar
    # 确保有读取权限
    chmod 644 plugin-s3-1.1.0.jar
    ```

3.  **重启服务**：
    ```bash
    docker compose restart gordon-blog
    ```

4.  **配置插件**：
    登录 Console 后台 -> 插件 -> 启用 `plugin-s3` -> 设置 -> 选择存储类型为 `Local`。

---

## 部署配置参考

### Docker Compose (PostgreSQL 版)

推荐使用 PostgreSQL 作为生产环境数据库。

`docker-compose.yaml`:

```yaml
services:
  gordon-blog:
    build:
      context: .
    container_name: gordon-blog
    restart: unless-stopped
    depends_on:
      gordon-db:
        condition: service_healthy
    networks:
      - gordon-net
    ports:
      - "${EXTERNAL_PORT:-8103}:8090"
    volumes:
      - ./xblog:/application/xblog2  # 宿主机 ./xblog 将包含 plugins, themes, logs 等目录
    environment:
      JVM_OPTS: "${JVM_OPTS:--Xmx256m -Xms256m}"
      TZ: "Asia/Shanghai"
      # 确保容器内工作目录与挂载点一致
      HALO_WORK_DIR: "/application/xblog2"
    command:
      - --spring.r2dbc.url=r2dbc:pool:postgresql://gordon-db/${POSTGRES_DB}
      - --spring.r2dbc.username=${POSTGRES_USER}
      - --spring.r2dbc.password=${POSTGRES_PASSWORD}
      - --spring.sql.init.platform=postgresql
      - --halo.external-url=http://localhost:${EXTERNAL_PORT:-8103}/
      - --halo.work-dir=/application/xblog2
    healthcheck:
      test: ["CMD", "curl", "-f", "http://localhost:8090/actuator/health/readiness"]
      interval: 30s
      timeout: 10s
      retries: 5
      start_period: 60s
    deploy:
      resources:
        limits:
          memory: 320M
        reservations:
          memory: 64M

  gordon-db:
    image: postgres:15-alpine
    container_name: gordon-db
    restart: unless-stopped
    ports:
      - "${DB_PORT:-15432}:5432"
    networks:
      - gordon-net
    volumes:
      - ./db:/var/lib/postgresql/data
    environment:
      POSTGRES_USER: ${POSTGRES_USER}
      POSTGRES_PASSWORD: ${POSTGRES_PASSWORD}
      POSTGRES_DB: ${POSTGRES_DB}
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U ${POSTGRES_USER:-gordon}"]
      interval: 10s
      timeout: 5s
      retries: 5
      start_period: 30s
    deploy:
      resources:
        limits:
          memory: 256M
        reservations:
          memory: 64M

networks:
  gordon-net:
    name: gordon-net
    driver: bridge
```

### 启动服务

```bash
# 首次启动或配置变更后
docker compose up -d --build
```

访问地址：[http://localhost:8103](http://localhost:8103)

---

## 维护与排查

### 检查端口占用
如果端口启动失败，检查占用情况：
```bash
# Linux / macOS
lsof -i :8103
# Windows PowerShell
netstat -ano | findstr 8103
```

### 资源监控
查看 Halo 的实时资源占用：
```bash
docker stats gordon-blog
```